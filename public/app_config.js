define([], function () {
    return {
        baseUrl : 'http://localhost:3000',
        layoutSelector: '#app-root',
        selector: '#route-selector',
        debug: true,
        title: 'Require + jquery + Handlebars + Router skeleton',
        apiRoot: '/api/v1/'
    };
});